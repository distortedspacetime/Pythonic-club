{
  "dependencies": {
    "node": "^20.2.0"
  },
  "homepage": "https://distortedspacetime.github.io/Pythonic-club/",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
